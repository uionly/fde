import { ArrowRight, BookMarked, Download, Library } from "lucide-react";
import type { Metadata } from "next";
import Link from "next/link";

import { getAllGlossaryEntries, getAllResources } from "@/lib/content";

export const metadata: Metadata = { title: "Resources" };
export default function ResourcesPage() { const resources = getAllResources(); const glossary = getAllGlossaryEntries(); return <div className="mx-auto max-w-[1100px] px-4 py-14 sm:px-6 sm:py-20 lg:px-8"><p className="font-mono text-xs font-semibold uppercase tracking-[0.16em] text-primary">Field reference</p><h1 className="mt-4 text-balance text-4xl font-semibold tracking-[-0.045em] sm:text-6xl">Bring the right artifact.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">Use practical templates during customer work and keep a shared technical vocabulary close at hand.</p><Link className="group mt-10 flex items-center gap-4 rounded-xl border bg-card p-5 hover:border-primary/35" href="/resources/glossary"><span className="grid size-10 place-items-center rounded-lg bg-accent text-primary"><BookMarked aria-hidden="true" className="size-5" /></span><div><h2 className="font-semibold group-hover:text-primary">FDE glossary</h2><p className="mt-1 text-sm text-muted-foreground">{glossary.length} field terms connected to lessons and experiments.</p></div><ArrowRight aria-hidden="true" className="ml-auto size-4 text-muted-foreground" /></Link><section className="mt-10"><div className="flex items-center gap-2"><Library aria-hidden="true" className="size-5 text-primary" /><h2 className="text-xl font-semibold">Downloadable templates</h2></div><div className="mt-5 grid gap-4 md:grid-cols-2">{resources.map((resource) => <Link className="group rounded-xl border bg-card p-5 hover:border-primary/35" href={`/resources/templates/${resource.slug}`} key={resource.id}><div className="flex items-center justify-between"><span className="font-mono text-[10px] uppercase tracking-[0.1em] text-primary">{resource.category} · {resource.format}</span><Download aria-hidden="true" className="size-4 text-muted-foreground" /></div><h3 className="mt-6 font-semibold group-hover:text-primary">{resource.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{resource.description}</p></Link>)}</div></section></div>; }
